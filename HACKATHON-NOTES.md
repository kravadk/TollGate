# Hackathon analysis — per-project, what each hackathon needs that we can add

> Generated 2026-05 from the live hackathon pages (or `agent-payments-x402-universal-tz-uk.md` §7 where the live page 405'd / is a JS SPA). Companion to `TRACK-PLAN.md` (which lists what's already built — §2c). The current build is a frontend demo with a real `server/` x402 gateway + MCP; the gap below is mostly **real on-chain integration + the per-track functions that make each submission credible**.

## Source links analysed
| # | Link | Workspace | Status |
|---|---|---|---|
| 1 | https://www.hackquest.io/hackathons/0G-APAC-Hackathon | 0g | ✓ fetched |
| 2 | https://arbitrum-london.hackquest.io/buildathons/Arbitrum-Open-House-London-Online-Buildathon → resolved via https://www.hackquest.io/hackathons/Arbitrum-Open-House-London-Online-Buildathon | arbitrum | ✓ fetched |
| 3 | https://hackathon.qie.digital/ | qie | ✓ fetched |
| 4 | https://dorahacks.io/hackathon/liquify/detail | liquify | ✗ 405 (DoraHacks SPA) — used ТЗ §7.2; **re-check** |
| 5 | https://dorahacks.io/hackathon/mantleturingtesthackathon2026/detail | mantle | ✓ **full announcement text supplied by the user 2026-05** (DoraHacks page itself 405's) — facts below are authoritative |
| 6 | https://eazo.ai/hackathon → **EAZO Global Hackathon 2026** | eazo | ✓ resolved (user-supplied 2026-05) — the ТЗ's tracks were correct (+ "Body Intelligence"); see §Eazo |
| 7 | https://www.deepsurge.xyz/hackathons/b587dc0c-4cb8-4e63-ada5-519df38103bf | deepsurge / **Sui** | ✓ resolved: **this UUID URL is the registration page for "Sui Overflow 2026"** (DeepSurge is the registration platform, not a separate hackathon). The ТЗ's `deepsurge.xyz/evefrontier2026` is a *different*, EVE-Frontier-themed Sui hackathon ("Sui × EVE Frontier 2026", $80K, Mar 11–31). See §DeepSurge / §Sui-Overflow. |
| 8 | https://luma.com/frw43jmv | — | ✗ page too large to fetch — **likely the EAZO Global Hackathon 2026 luma listing** (its registration is on lu.ma) — confirm. |
| 9 | https://ai.hackberkeley.org/ | berkeley | ✓ fetched |

## Hackathon facts

| Workspace | Hackathon | Prize | Key dates | Tracks (verbatim where known) | Hard requirement / required tech |
|---|---|---|---|---|---|
| **0g** | 0G APAC Hackathon | $150K (USDT + 0G credits): 1st $45K / 2nd $35K / 3rd $20K + 10×$3.7K Excellence + 10×$1.3K Community | reg Mar 19 · checkpoint Apr · HK Web3 Festival demo Apr 22 · **submit May 16 (23:59 UTC+8)** · winners ~early May | T1 **Agentic Infrastructure & OpenClaw Lab** · T2 **Agentic Trading Arena (Verifiable Finance)** — "Sealed Inference and TEE-based execution to ensure execution privacy and mitigate front-running" · T3 **Agentic Economy & Autonomous Applications** · T4 **Web 4.0 Open Innovation (The Wildcard)** — SocialFi/gaming/DePIN on 0G storage · T5 **Privacy & Sovereign Infrastructure** | **Mandatory: a real 0G mainnet contract address + a 0G Explorer link showing verified on-chain activity + ≥1 0G component** (0G Storage / 0G Compute / 0G Chain / Agent ID / privacy or secure-execution features). Demo video ≤3 min, README, public GitHub, **public X post with #0GHackathon #BuildOn0G** — all required. Encouraged: integrate **OpenClaw** for orchestration; 0G Compute for fine-tune/inference; 0G Storage for state persistence + long-context memory. Judging: 0G integration depth & innovation · technical completeness · product value · UX/demo · team/docs. |
| **arbitrum** | Arbitrum Open House London — Online Buildathon (Arbitrum Foundation) | $115K USDC: **Overall Prize $70K** (40/20/10) · **Best Agentic Project $15K** (7/5/3) · **Grants up to $30K** (milestone-based, discretionary) | reg **May 25** · **submit Jun 14** · rewards Jun 17 | "Overall Prize" · "Best Agentic Project" · "Grants" | **Must deploy on an Arbitrum chain** — Arbitrum Sepolia / Arbitrum One / **Robinhood Chain** / Orbit chains. **≥1 prize reserved for Robinhood Chain projects, ≥1 for Arbitrum One.** Solidity + **Rust via Stylus**. Judging: smart-contract quality (best practices, minimal vulns) · PMF & user retention · innovation/creativity · real problem solving. |
| **qie** | QIE Blockchain Hackathon 2026 | $20.5K (50% USDT / 50% QIE): 1st $2.5K / 2nd $1.5K / 3rd $1K + 5×$500 per-track Excellence + **$1.25K QIE bonus for 100+ active users post-hackathon** | reg Apr 15–May 15 · build May 16–Jun 14 · **submit Jun 15–19** · judging Jun 20–24 · results Jun 25 | "DeFi & Payments" · "AI + Web3" · "Gaming & Metaverse" · "Infrastructure & Tools" · "Social & Community" | **QIE testnet (Chain ID 1983)**, RPC `https://rpc1testnet.qie.digital/`, Solidity (Hardhat/Foundry), **QIE Wallet for auth**, **deployed + verified contract on `testnet.qie.digital`**. Optional ecosystem: QIE Stable Coin, QIE Dex, QIE Pass (multi-component = bonus consideration). Judging: originality/innovation · real-world usefulness · product quality & UX · QIE ecosystem integration · adoption potential. **Anti-abuse: forked/recycled code, copied projects, simple clones, AI-only projects, or submissions without a working demo/deployment → immediate disqualification.** |
| **liquify** | DoraHacks × Liquify *(facts from ТЗ §7.2 — live page 405'd, re-check)* | (not fetched) | (not fetched) | "Next-Gen Trading Tool with x402 Integration" · "Advanced Wallet Analysis Tool" · "Seamless DeFi Tax Reporting Tool" | Build on the **Liquify indexer / DeFi data API**; **x402 integration** is the headline track. Demo endpoints in the ТЗ: `wallet-risk`, `trading-signal`, `tax-classifier`. |
| **mantle** | **The Turing Test Hackathon 2026** — Mantle, co-hosted with **Bybit · Byreal · Blockchain for Good Alliance (BGA)**; supported by DoraHacks + HackQuest. "First on-chain benchmark of AI-agent performance at scale." | **$120K total** — Phase 1 "ClawHack" $20K (Apr 15–30, already over) + Phase 2 "AI Awakening" $100K. Phase-2 prizes: **Grand Champion** + per-track First Prizes + **Best UI/UX Award** + **Community Voting** (X) + **"20 Project Deployment Award"** (first 20 to clear the bar). | Phase 1 Apr 15–30 *(done)*; Phase 2 "AI Awakening" — register on the Mantle DoraHacks page *(exact Phase-2 dates: check DoraHacks)*. Live-streamed globally. | **6 Phase-2 tracks:** "AI Trading & Strategy" (AI quant bots + macro smart contracts; Python/Solidity templates + Bybit API; *sponsored by BGA*) · "AI Alpha & Data" (smart-money tracking + on-chain anomaly detection via Telegram/Discord; *Alpha & Data First Prize sponsored by Mirana Ventures* — two paths: Human-driven Data & Analytics OR AI-driven Trading Strategy; must use **Mantle on-chain data as core source**) · "AI x RWA" (dynamic yield + automated risk mgmt for **USDY / mETH** on Mantle's RWA infra; *supported by Mantle Network* — two paths: Human-driven RWA Infrastructure OR AI-driven RWA Application; must involve a real-world asset) · "Consumer & Viral DApps" (gamified trading UIs, shareable consumer apps) · "AI DevTools" (smart **gas-optimisation** tools, **Mantle-specific audit assistants**) · "Agentic Wallets & Economy" (*sponsored by Byreal* — must use ≥1 of **Byreal Agent Skills / Byreal Perps CLI / RealClaw**; two paths: Human-driven "DeFi Deep Dive" OR AI-driven "RealClaw Real-Life Expansion" — e.g. Personal CFO Agent, on-chain life manager, health-data mgmt, everyday decision assistant; deploy on Mantle **or Solana**). | **Hard gates:** deployed on **Mantle Network** · open-source repo + runnable demo + project pitch · nominated from ≥1 track. **Grand Champion scoring:** Technical Depth 30% (AI×on-chain integration, architecture completeness, code quality) · Innovation 25% (new AI×Web3 paradigm) · **Mantle Ecosystem Contribution 25%** (substantive use of Mantle + long-term value) · Product Completeness 20% (runnable demo, UX, scalability). **Two defining features to lean into: ① on-chain benchmarking (every agent decision recorded on Mantle) · ② ERC-8004 agent identity — every participating AI agent gets a unique identity NFT via ERC-8004, an on-chain record of its achievements/reputation.** **Best UI/UX scoring:** Visual 30% · Interaction & Flow 30% · AI Interaction Design 25% · Accessibility 15% (beginner-friendly, lower the Web3 barrier) — needs a runnable frontend + demo video/link. **"20 Project Deployment Award" bar (no judge scoring — just clear all):** contract deployed on **Mantle Mainnet or Testnet + verified on Mantle Explorer** · ≥1 AI-powered function callable **on-chain** (agent trigger / inference result written on-chain / automated execution) · frontend demo **publicly accessible (not localhost)** · deployment address in the DoraHacks submission · demo video **≥2 min** walking through the core use case · open-source GitHub repo with README (setup, architecture, deployed contract address). Judges: Allora Network, BGA, Nansen, Z.ai, Four Pillar, Animoca Brands, DoraHacks, Elfa AI, Virtuals Protocol, Hashed, Caladan + a HKU academic. Tooling: **Byreal Skills CLI**, Byreal Agent Skills, Byreal Perps CLI, **RealClaw** (OpenClaw-based agent with Byreal Skills pre-installed; extensible to non-DeFi). Phase-1 venues: Merchant Moe, Agni Finance, Fluxion via RealClaw. | user-supplied announcement ✓ |
| **eazo** | **EAZO Global Hackathon 2026** ("Where Imagination Ships" — host **ASI X Inc.**; #UnlabelYourself #BeTheCreator; hosted by Eazo.ai · Streaml · Gen · AI Valley) — *(user-supplied 2026-05; the earlier "one-day, no tracks" read was incomplete)* | **$300K total · 243 cash winners · ~30% cash-win rate.** Global Awards $90K (Grand Prize $50K · People's Choice $25K · Builder's Choice $15K) + 4 equal regional pools $52.5K each (SF/NYC/Shanghai/Online): 1st $8K / 2nd $5K / 3rd $3K / 4th–10th $1.2K / 11th–30th $700 / 31st–60th $470. + 6 Achievement Badges ×4 regions (Trailblazer = first 150 to publish · 100 Club = 100+ real users · Dark Horse = 1000+ users · Speed Launch = published within 3h · Breakout = highest non-community user ratio · Social Pick = most social shares) + Eazo **Skills Marketplace revenue share** post-hackathon. | **May 23–24, 2026 · 48h** · SF · NYC · Shanghai + Online (each city local time, kick-off 10:00 AM May 23; submissions close 9:00 AM May 24 — voting opens on submit, so submit early; Demo Day May 24 9:00 AM, ≤3 min each; awards 7:00 PM PT). | **5 tracks (the ТЗ was right):** "Superparent" (modern family coordination, memory, decision-making, automation) · "AI Companion" (daily AI companionship + focus tools, incl. ADHD/ASD support) · "Life OS" (personal productivity, execution, planning, self-management) · "Body Intelligence" (health tracking, fitness, recovery, rehab, body-aware tools) · "Wildcard" (open-ended). | **HARD CONSTRAINT: products must be built using *Eazo Creator* (a no-code AI builder) and published through it to the Eazo platform** — our React/Vite app does not qualify as a literal submission; the Eazo workspace is a *conceptual fit* for the AI Companion / Life OS / Superparent / Body Intelligence tracks but cannot be submitted as-is. Teams 1–3. Judging: **50% Eazo platform Likes + 40% Expert Panel (6 judges: 2 top-tier VCs, 2 AI founders, 1 "Coding Agent" authority, 1 "Design Agent" authority) + 10% Peer Vote** (3 stickers each, not your own). 5 scoring dimensions, 20% each: Product Completeness ("real and usable — not a demo") · Innovation · Technical Execution ("depth & quality of Eazo Creator usage") · Design & Experience · Commercial Potential. "What you submit is a real, live product anyone can open and use." Online is an independent 4th region (equal). | user-supplied ✓ |
| **berkeley** | UC Berkeley AI Hackathon 2026 | $100K (cash + tech prizes); 300+ projects, 1300+ hackers | apply by **Apr 26 (priority) / May 17 (regular)** · **event Jun 20–21** (24h, in-person, MLK Jr. Building, UC Berkeley) | "Ddoski's World" — social impact (EdTech, civic, environmental, inequity) · "Ddoski's Toolbox" — dev/creator tools (utilities, APIs, automation, workflow, design aids) — judged on **"usefulness, usability, and execution over concept"** · "Ddoski's Lab" — science/eng (health tech, hardware, biotech, data tools) — **"technical depth and real-world application"** · "Ddoski's Playground" — creative (games, generative art, experimental interfaces) — **"originality, execution, and compelling experience"** | All implementation during the 24h event (ideation beforehand OK). Sponsor APIs/hardware available. Undergrad/grad/recent-grad, 18+, teams ≤4. |
| **deepsurge** *(workspace = "Frontier Intel Market")* | Two related Sui-ecosystem hackathons reachable via **DeepSurge** (the registration platform): **(a) Sui × EVE Frontier 2026** ("A Toolkit for Civilization") and **(b) Sui Overflow 2026** *(see §Sui-Overflow — the `deepsurge.xyz/hackathons/b587dc0c-…` UUID URL is Overflow's registration)*. | (a) $80K: 1st $15K / 2nd $7.5K / 3rd $5K + 5×$5K category champions (+ EVE Points, SUI, FanFest passes). (b) Overflow: see §Sui-Overflow ($500K+). | (a) begins **Mar 11** · **submit Mar 31** · voting Apr 1–15 · judging Apr 15–22 · winners Apr 24. (b) see §Sui-Overflow (May 7 – Jun 21). | **(a) EVE-Frontier tracks:** "Utility" (mods that change how players survive/coordinate/explore/compete) · "Technical Implementation" · "Creative" · "Weirdest Idea" · "Live Frontier Integration" (deployed & functioning in **Stillness**, interacting with live players). | **EVE Frontier integration** (on-chain world data / Frontier systems). The Sui variants want **Move / Sui**. The "Frontier Intel Market" workspace fits (a)'s Utility / Live-Frontier tracks; for (b) Sui Overflow, the **Agentic Web** + **Walrus** tracks fit the *core* x402-agent product far better (see §Sui-Overflow) — consider pointing this workspace (or a 9th) at Overflow. |

## Per-workspace gap analysis — functions still needed (that I can add)

Legend: **P0** fix/reframe · **P1** strongest score-mover · **P2** nice differentiator · **P3** stretch. (✅ = already built — listed for reference.)

### 0G — `/app/0g`
✅ Built: `InferenceJobRunner` (+sealed/TEE toggle + batch), `StoragePinWidget` (+agent-memory snapshot/restore), `AgentIdRegistry`, `RevenueSplitConsole`, `SealedPayloadVault`.
- **P1 · Real 0G integration — the submission dealbreaker.** Wire `@0glabs/0g-ts-sdk`: the pin / memory-snapshot widgets do a *real* 0G Storage upload → real content roots + a clickable **0G Explorer link** in the receipt; OR a real 0G Compute inference call. Plus deploy a tiny `AgentReceiptRegistry.sol` to **0G mainnet** (records receipt hashes on-chain) → put the contract address + Explorer link in the Overview tab + README. *(Needs keys/RPC/deploy → its own sub-plan.)* → satisfies the mandatory "real 0G mainnet contract + Explorer link + 0G component"; → T1/T3.
- **P1 · OpenClaw skill manifest.** The hackathon explicitly encourages OpenClaw orchestration: ship an OpenClaw skill descriptor (so an OpenClaw agent can call our gateway) + a "Run via OpenClaw" button on the Compute/Agents tab. → T1 (Agentic Infrastructure & OpenClaw Lab).
- **P1 · TEE attestation verifier.** Given an `attestationId` from a sealed inference → "Verify attestation" → ✓ + the TEE quote summary + receipt. Makes the sealed-inference loop *verifiable*, not just labelled. → T2 (Trading Arena, front-running mitigation) / T5 (Privacy).
- **P2 · DePIN / storage scale test.** Bulk-pin N agent-memory blobs → throughput + total bytes on 0G Storage + per-blob Explorer links. → T4 (Web 4.0 — "0G's decentralized storage for real-world scaling").
- **P3 · SocialFi agent feed.** Agents publish content (pinned to 0G Storage) and micro-tip each other. → T3/T4.

### Arbitrum — `/app/arbitrum`
✅ Built: `UsdcTransferWidget` (real ERC-20 transfer on Arbitrum Sepolia), `AgentServiceRegistry`, `InteractiveEscrow`, `OrbitMonitorPanel` (interactive paid queries), `SpendRulesEditor`.
- **P1 · Real `AgentEscrow.sol` on Arbitrum Sepolia — the dealbreaker.** Open / release / refund, single-claim, no admin function, clear failure states. Wire `InteractiveEscrow` to it (real txs + Explorer links). "Must deploy on an Arbitrum chain" + judging weighs smart-contract quality & security → this is what makes "Best Agentic Project" credible. *(Sub-plan: `contracts/` + Hardhat/Foundry deploy + verify.)*
- **P1 · Real x402 → real USDC settlement on Arbitrum.** The `PaymentModal` / `LiveGatewayPanel` on the Arbitrum workspace does a real USDC `transfer` (we already have `sendErc20Transfer`) → `server/` verifies the tx hash before unlocking → the full **402 → on-chain pay → verify → unlock** loop, on a real chain. → Best Agentic / Overall.
- **P2 · Robinhood Chain adapter.** Add "Robinhood Chain" to the chain selector + `WORKSPACE_CHAIN` (`src/wallet.tsx`) + a "deploy/test on Robinhood Chain" path → eligible for the **reserved Robinhood-Chain prize**.
- **P2 · Stylus contract snippet.** Show the escrow/registry in **Rust/Stylus** alongside Solidity in the Integration foot. → innovation / DevTools angle.
- **P3 · `MultiSend.sol` batch-payout contract** — for the batch-payout widget.

### QIE — `/app/qie`
✅ Built: `CheckoutLinkBuilder` (+settle + split fan-out), `SettlementSplitConfig`, `AgentWalletConsole`, `QiePassIssuer` (+access check), `SwapQuoteDesk`.
- **P1 · Real QIE testnet deploy — the dealbreaker.** Deploy `Checkout.sol` (create invoice → pay → split fan-out) + `QiePass.sol` (mint tiered NFT pass, check tier) to **QIE testnet (Chain ID 1983)**, verified on `testnet.qie.digital`; wire `CheckoutLinkBuilder` + `QiePassIssuer` to real txs; add **QIE Wallet connect** (1983 in `WORKSPACE_CHAIN` / `tokensForChain` / `NATIVE_SYMBOL` in `src/wallet.tsx`). → required + judging weighs QIE-ecosystem integration; Checkout + Pass + Wallet = the multi-component bonus.
- **P1 · POS / commerce plugin.** The "DeFi & Payments" track explicitly lists "POS integrations, commerce plugins": "Generate POS widget" → an embeddable `<script>` snippet + a public hosted checkout page for a merchant, paid in QIE/USDT. → DeFi & Payments.
- **P2 · P2E item shop.** Buy game items with QIE; the receipt is the item grant. Cheap; opens the **Gaming & Metaverse** track (currently no fit).
- **P2 · Creator tip-jar / memberships.** QIE-Pass-gated subscriber tiers + recurring QIE payments. → Social & Community.
- **P3 · QIEDEX LP manager** — add/remove liquidity (real contract calls if a QIEDEX testnet contract is available).

### Liquify — `/app/liquify`
✅ Built: `TradingSignalDesk`, `WalletRiskAnalyzer` + `ApprovalRevokePlanner`, `TaxLotCalculator` + `TaxExport`, `LiveGatewayPanel`.
- **P1 · Wire to the real Liquify data API behind x402.** Replace the deterministic data with real Liquify indexer calls (wallet history / balances / txs / pool data), gated by the 402 handshake — the literal "Next-Gen Trading Tool **with x402 Integration**" claim. *(Needs the Liquify API key/docs — get from the hackathon page.)*
- **P1 · Live ticker board.** Drifting prices + per-pair buy/sell signal + alert-on-cross → metered per call. → Next-Gen Trading Tool.
- **P2 · Multi-wallet PnL / counterparty graph.** 2+ addresses → realized/unrealized PnL + shared-counterparty edges. → Advanced Wallet Analysis Tool.
- **P2 · Wash-sale detector + per-jurisdiction summary.** Flag wash sales in the lot set, group by jurisdiction tags, export per-jurisdiction CSV. → Seamless DeFi Tax Reporting Tool.
- **P3 · Address-cluster / entity-label lookup** — entity clustering + labels (exchange / defi / drainer).

### Mantle — `/app/mantle`  *(updated 2026-05 from the full Turing Test announcement — much richer than the ТЗ assumed)*
✅ Built: `MantleEconomyLoop` (earn x402 → store → deploy surplus → mETH yield), `AlphaDesk`, `RwaRegistry` (+risk report), `YieldBoard`, `BacktestRunner`.
The two hackathon flagships to lean into: **on-chain benchmarking** (every agent decision recorded on Mantle) and **ERC-8004 agent identity NFTs**. Easiest concrete target: the **"20 Project Deployment Award"** — verified contract on Mantle Mainnet/Testnet + ≥1 AI function callable on-chain + public frontend + ≥2-min demo video + README with the deployed address.
- **P1 · ERC-8004 agent identity registry on Mantle (the flagship + dealbreaker).** Deploy an **ERC-8004** identity contract to Mantle (or Mantle Sepolia, verified on Mantle Explorer) → a `MantleAgentRegistry` widget: "Register agent" → mints an **identity NFT** for the agent, records its track + on-chain reputation; each subsequent receipt (`mantle.*`) bumps the agent's on-chain score. → satisfies "ERC-8004 agent identity standard" + "on-chain benchmarking" + the 20-project bar + Agentic Wallets & Economy. *(Sub-plan: `contracts/` + deploy + verify.)*
- **P1 · Byreal Skills / RealClaw integration (required for the Agentic Economy track).** That track *mandates* using ≥1 of **Byreal Agent Skills / Byreal Perps CLI / RealClaw**. Add a "Run via RealClaw" path on the Agent Wallets / Strategy tabs (RealClaw = OpenClaw-based agent with Byreal Skills pre-installed) + a "Byreal Skills CLI" snippet in the Integration foot; the AI-driven "RealClaw Real-Life Expansion" sub-path explicitly wants **Personal CFO Agent / on-chain life manager / health-data mgmt / everyday decision assistant** — our `MantleEconomyLoop` reframed as a "Personal CFO Agent" fits.
- **P1 · On-chain AI-callable function (the 20-project bar).** Wire `MantleEconomyLoop`'s "Deploy surplus → mETH" (and/or the backtest runner) to a real `AgentVault.sol` on Mantle so an **AI agent can trigger it on-chain** (agent trigger / automated execution / inference result written on-chain) → the deployment-award checklist.
- **P1 · Strategy deploy-to-agent.** Passing backtest → "Deploy as live agent strategy" → a live-strategy card that runs rotation ticks → receipts → the economy loop consumes it. → AI Trading & Strategy + Agentic Wallets & Economy.
- **P2 · Smart-money tracking agent + Mantle protocol data dashboard.** "AI Alpha & Data" (First Prize sponsored by Mirana Ventures) explicitly wants these, using **Mantle on-chain data as the core source**: a "track wallet 0x…" widget → labels + recent moves + alert subscription; a dashboard of Merchant Moe / Agni / Fluxion / mETH pool stats. → AI Alpha & Data.
- **P2 · RWA portfolio agent + yield aggregator.** "AI x RWA" wants "intelligent RWA portfolio management agent" + "RWA yield aggregator" + "automated KYC/compliance" for **USDY / mETH**: add a "rebalance my RWA portfolio" action to `RwaRegistry` + a compliance-check stub. → AI x RWA.
- **P2 · Gas-optimisation / audit-assistant tool.** "AI DevTools" wants "smart gas optimisation tools" + "Mantle-specific audit assistants": a "paste a tx / contract → AI gas report + audit findings + receipt" widget. → AI DevTools.
- **P2 · Gamified / shareable trading interface.** "Consumer & Viral DApps" wants "gamified trading interfaces and shareable consumer applications": a "trade-of-the-week" card with a share link + a leaderboard. → Consumer & Viral DApps.
- **P2 · Backtest-as-paid-API publisher.** Expose the backtest runner as a paid x402 API + a code snippet ("register as service"). → AI DevTools.
- **P3 · Projected-yield calculator** (asset + amount + window → curve, mETH vs USDY vs RWA basket). → AI x RWA.
- **Best UI/UX Award is winnable for free** — the workspace is already polished; the AI-interaction (companion-style prompts) + accessibility (the QuickCallPanel / "Try the demo agent" one-click flow) score against the 30/30/25/15 rubric. Just ensure a public deployed link + a demo video.

### Eazo — `/app/eazo`  *(updated 2026-05 — the ТЗ's tracks were correct: Superparent / AI Companion / Life OS / Wildcard + a new "Body Intelligence")*
✅ Built: `EazoSubManager`, `EazoCompanionPanel`, `EazoBudgetTracker`, `EazoApprovalRules`, `EazoDailyOps`.
- **⚠ HARD CONSTRAINT (read first):** EAZO Global Hackathon 2026 requires products to be **built with *Eazo Creator* (a no-code AI builder) and published to the Eazo platform** — our React/Vite app is **not a literal submission**. Treat the Eazo workspace as a *concept showcase* of what the AI-companion product would do (and, if you want a real Eazo entry, rebuild the core flows inside Eazo Creator). The widgets below still strengthen the concept demo.
- **P1 · Companion chat surface** (matches "AI Companion" track + the judges' "AI Interaction Design" dimension). A chat panel in the AI Companion tab: type a goal ("cut my subscription spend 30%") → the companion proposes a plan of actions → approve to execute (each → receipt). Turns the static suggestion cards into a conversational product.
- **P1 · Body Intelligence widget** (the new track — currently no fit). A health/fitness companion card: log a metric (sleep / steps / HR) → AI suggestion (rest day / hydration / "book a physio") with an "approve action" → receipt; weekly recovery bars. → Body Intelligence.
- **P1 · Tool-purchase console.** The companion buys a paid AI tool within budget → receipt; "tools used this week" counter. (Eazo's "Skills Marketplace" angle.)
- **P2 · Family-member sub-budgets** (→ Superparent). Add a member → capped budget → receipt per member spend; the modern-family-coordination/memory framing the Superparent track wants.
- **P2 · Life OS daily-ops triggers** already exist (`EazoDailyOps`) — extend with a "plan my week" generator → schedule cards. → Life OS.
- **P3 · One-line quick-add** — add a subscription/expense/reminder by typing one sentence.
- **Polish:** the judges weight Product Completeness / Design & Experience / Commercial Potential 20% each + 50% platform likes — make the Overview a one-screen "what this is" hero and ensure a public deployed link + a ≤3-min demo video.

### Berkeley — `/app/berkeley`
✅ Built: `PlaygroundInspector`, `PaidToolsGrid`, `TxExplainerPanel` (+paid decode of arbitrary calldata), `AgentDebuggerPanel` (+paid re-run of a scenario).
- **P1 · Build-a-tool wizard.** Define a tool (name / price / input schema) → publish → it appears in the Paid Tools grid → others pay & run it → receipts. "A toolbox where AI agents publish & pay for each other's tools" = a clean **Ddoski's Toolbox** entry (judged on usefulness / usability / execution).
- **P1 · Tool output panel + per-tool run history.** After paying, show the deterministic JSON result + receiptId + a run log per tool (currently `PaidToolsGrid` just marks "paid"). → Toolbox usability.
- **P2 · Playground: agent-payment sandbox game.** Fund an agent, it spends on tools to complete a quest, you watch the receipts pile up. → **Ddoski's Playground** ("compelling experience").
- **P2 · Tx-explainer before/after balance simulation.** "Before 1.23 ETH · After 0.98 ETH" + the diff highlighted. → Toolbox.
- **P3 · Export the agent-debugger timeline as JSON.**

### DeepSurge — `/app/deepsurge`
✅ Built: `FrontierIntelQuery`, `RouteRiskScorer`, `ResourceMapQuery`, `AlertSubscriptions`.
- **P1 · Live Frontier integration.** Wire to EVE Frontier's on-chain world data — the "Live Frontier Integration" track wants mods "deployed and functioning in Stillness, interacting with the live world and real players": real solar-system / market / fuel data instead of deterministic. *(Needs the EVE Frontier API/contracts; the Sui variant → Move.)*
- **P1 · Trade escrow.** Open an escrow for a Frontier player-to-player trade until delivery confirms → release/refund → receipts. "An escrow rail for Frontier trades." → Utility.
- **P2 · Route optimizer.** From/to + cargo → cheapest/safest multi-jump route + fuel cost + receipt (richer than the current risk scorer). → Utility / Technical Implementation.
- **P2 · Market oracle query.** Commodity prices / hub spreads per query → receipt. → Live Frontier Integration.
- **P3 · "Weirdest idea" easter egg** — a meme-y paid "Frontier fortune" endpoint. → Weirdest Idea.

## Sui Overflow 2026 — newly identified (possible 9th workspace, or repoint `deepsurge`)
*(The `deepsurge.xyz/hackathons/b587dc0c-…` URL the user originally listed is Overflow's registration page.)* Sui's flagship online hackathon, **May 7 – Jun 21 build · submit Jun 21 · shortlist Jul 8 · Demo Day Jul 20–21 · winners Aug 27**. **$500K+** prizes (Headline Partner: **Walrus**; Track Sponsors: DeepBook, OpenZeppelin, OtterSec, Scallop). **50/50 split prize: 50% on win, 50% after mainnet deploy** (or 100% upfront if already on mainnet). Required: **Move / Sui**, deployed to Sui testnet or mainnet, public GitHub during judging, demo video ≤5 min, ≥1 team member passes KYC. Judging: **Real-World Application 50% · Product & UX 20% · Technical Implementation 20% · Presentation & Vision 10%.** AI-assisted dev allowed/expected.
Core tracks (each 1st $30K / 2nd $15K / 3rd $10K / 4th $7.5K): **"Agentic Web"** — "AI-native agents and autonomous workflows that deeply leverage Sui primitives … beyond simple integrations" · **"DeFi & Payments"** — "programmable payment systems and financial applications on Sui that move, manage, and transform money intelligently". Special tracks (1st $35K / 2nd $15K / 3rd $7.5K / 4th $5K + $7.5K honorable mentions): **"Walrus"** — "AI agents and agentic workflows powered by **Walrus as a verifiable data and memory layer**" (Walrus = Sui's verifiable-data platform; MemWal = Walrus Memory; Seal = privacy layer) · **"DeepBook"** — apps/services/vaults/bots/analytics on Sui's onchain liquidity layer. + University Award 10×$2.5K (≥50% students).
**Why this fits our core product:** "Agentic Web" + "Walrus (verifiable data + memory layer)" is essentially the **same product as the 0G workspace** (paid AI agents · agent identity · agent memory pinned to a verifiable storage layer · x402 micropayments) — just on Sui/Move with Walrus instead of 0G Storage, and DeepBook instead of a generic DEX. A 9th `sui` workspace (or repurposing `deepsurge`) would be: agent registry + x402 gateway + agent-memory snapshots to **Walrus** (real `Walrus TS SDK` / MemWal) + DeepBook market data — and "DeFi & Payments" is covered by the same `PaymentModal`/checkout/escrow core. **Effort:** the UI/widget pattern transfers 1:1; the new work is the Move contracts + Walrus/MemWal SDK + DeepBook integration. **This is a strong, high-prize, high-fit option — flag for the user.**

## Cross-cutting — the #1 thing all the on-chain hackathons want that we lack
**Real on-chain deployment + Explorer link.** `server/` already returns a real `402`; the next leap — and the biggest score multiplier for 0G / Arbitrum / QIE / Mantle — is **one tiny verified contract per chain** + wiring the relevant widget to it:
- `AgentReceiptRegistry.sol` on **0G mainnet** (records receipt hashes) — the mandatory 0G submission artifact.
- `AgentEscrow.sol` on **Arbitrum Sepolia** — wires `InteractiveEscrow`.
- `Checkout.sol` + `QiePass.sol` on **QIE testnet 1983** — wires `CheckoutLinkBuilder` + `QiePassIssuer`.
- An **ERC-8004 agent-identity contract** + `AgentVault.sol` on **Mantle** (or Mantle Sepolia, verified on Mantle Explorer) — wires `MantleAgentRegistry` (identity NFTs + on-chain reputation) and `MantleEconomyLoop`'s "deploy surplus → mETH" so an AI agent can trigger it on-chain. This + a public frontend + a ≥2-min demo video + a README with the deployed address = the **"20 Project Deployment Award"** checklist (no judge scoring, just clear all the bars). Mantle's hackathon explicitly uses **ERC-8004** + on-chain benchmarking as its defining features — leaning into those is the highest-leverage move there. Agentic Economy track *requires* using Byreal Agent Skills / Byreal Perps CLI / RealClaw.
- Plus the per-chain wallet/network entries in `src/wallet.tsx` (`WORKSPACE_CHAIN`, `tokensForChain`, `NATIVE_SYMBOL`, `EXPLORERS`).

This is its own follow-up implementation plan, scoped chain-by-chain (keys, RPC, deploy, ABI in repo, README addresses, verified contract). Everything else in this doc is client-side and reuses the existing widget pattern (`emitReceipt` / `useLocalStore` / `deterministicScore`).

## Track combos (which tracks a single submission can claim)
- **0G:** T1 Agentic Infra ⊕ T3 Agentic Economy (Agent ID + self-custodial wallet + revenue split + micropayments = one surface) · T2 Trading Arena ⊕ T5 Privacy (Sealed Inference / TEE = a privacy primitive) · T3 ⊕ T4 (agent memory pinned to 0G Storage at scale).
- **Liquify:** Next-Gen Trading ⊕ Advanced Wallet Analysis (a desk that also scores the wallet you trade from) · Wallet Analysis ⊕ Tax Reporting (same indexed data → risk report + tax-lot calc).
- **QIE:** DeFi & Payments ⊕ Social & Community ⊕ AI + Web3 (Pass-gated creator checkout that an agent settles) · + Gaming via the P2E item shop.
- **Arbitrum:** Best Agentic ⊕ Overall ⊕ Grants (agent-services registry + USDC settlement + escrow + spend-rules = "agentic payments platform on Arbitrum") · + Robinhood-Chain reserved prize via the adapter.
- **Mantle:** Agentic Wallets ⊕ AI Trading & Strategy (economy loop needs a strategy; backtest + yield board feed it) · AI Alpha & Data ⊕ AI x RWA ⊕ AI DevTools (alpha desk → RWA registry → backtest-as-API).
- **Berkeley:** Ddoski's Toolbox ⊕ Playground (build-a-tool wizard + paid-tools grid + the sandbox game).
- **DeepSurge:** Utility ⊕ Live Frontier Integration ⊕ Technical Implementation (a metered Frontier API pulling live data over a real x402 handshake hits all three).
- **Cross-workspace:** "Agentic" = 0G + Arbitrum + Mantle (same agent-identity/wallet/policy/receipts primitive — build once in `core`, claim three times) · "AI data feed" = Liquify + Mantle + DeepSurge (`TradingSignalDesk` / `AlphaDesk` / `FrontierIntelQuery` — one pattern, three submissions) · "payments/checkout" = QIE + Arbitrum + Eazo (`PaymentModal` + `emitReceipt` core).

## Open questions / resolved
1. ✅ **`luma.com/frw43jmv`** — almost certainly the **EAZO Global Hackathon 2026** lu.ma listing (its registration is on lu.ma). Confirm if you want it cross-linked.
2. ✅ **Eazo** — resolved: it's the **EAZO Global Hackathon 2026** (ASI X Inc.), May 23–24, 48h, SF/NYC/SH+Online, $300K — and the ТЗ's tracks (AI Companion / Life OS / Superparent / Wildcard) were right, plus "Body Intelligence". The hard part: it must be built with **Eazo Creator** (no-code), so our app is a concept showcase, not a literal submission.
3. **Liquify DoraHacks page** — the live `dorahacks.io/hackathon/liquify/detail` URL 405'd; the facts above are from the ТЗ. If you have the current track list / API docs / prize structure, drop them and I'll update. *(Mantle ✓ resolved — only the exact Phase-2 dates + the byreal-agent-skills / byreal-perps-cli / realclaw repo links are still TBD.)*
4. ✅ **DeepSurge** — resolved: **DeepSurge is the registration platform**, not a hackathon org. The UUID URL = **Sui Overflow 2026** registration (see §Sui-Overflow). The ТЗ's `deepsurge.xyz/evefrontier2026` is a separate **"Sui × EVE Frontier 2026"** hackathon. **Decide:** keep the `deepsurge`/"Frontier Intel Market" workspace pointed at EVE Frontier, or repoint it (or add a 9th `sui` workspace) at **Sui Overflow's Agentic Web + Walrus tracks** — the latter is a much higher-prize ($500K+) and much closer fit to the core x402-agent product.

## Next step
A follow-up implementation plan picks the **P1** items — starting with the per-chain real-contract deployments (0G mainnet first, then Arbitrum Sepolia, then QIE testnet 1983) — scoped chain-by-chain. The P1/P2 client-side widgets (OpenClaw button, TEE verifier, POS plugin, live ticker, strategy-deploy, companion chat, build-a-tool wizard, trade escrow, …) are straightforward additions in `WorkspaceDashboard.tsx` using the existing pattern.
